# Copie este arquivo para .env e configure o ambiente da API.
VITE_API_URL=

# Modo de autenticacao: "bearer" (padrao, contrato atual da API) ou "cookie".
# Use "cookie" somente quando o backend emitir o cookie httpOnly e responder
# Access-Control-Allow-Credentials com origem explicita.
VITE_AUTH_MODE=bearer
